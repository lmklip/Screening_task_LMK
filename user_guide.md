

```markdown
# Руководство пользователя OrderManager

## Как оформить заказ через API

### Шаг 1: Просмотр каталога продуктов

Откройте в браузере или через curl:

```
http://127.0.0.1:8000/products
```

Вы увидите список доступных товаров с информацией:
- Название товара
- Категория
- Цена
- Остаток на складе

**Пример ответа:**
```
{
  "total": 300,
  "page": 1,
  "per_page": 10,
  "products": [
    {
      "product_id": 1,
      "name": "Speed Item 1",
      "category": "Electronics",
      "price": 467.15,
      "stock": 45
    }
  ]
}
```

---

### Шаг 2: Фильтрация товаров (опционально)

Вы можете отфильтровать товары по категории или цене:

**Пример: товары в категории Electronics**
```
http://127.0.0.1:8000/products?category=Electronics
```

**Пример: товары дешевле 500**
```
http://127.0.0.1:8000/products?price_max=500
```

---

### Шаг 3: Создание заказа

Чтобы создать заказ, отправьте POST запрос с данными:
- **customer_id** — ваш ID клиента (1-200)
- **items** — список товаров (product_id и количество)

**Через Swagger UI (визуальный интерфейс):**

1. Откройте http://127.0.0.1:8000/docs
2. Найдите раздел **Orders** → **POST /orders**
3. Нажмите **Try it out**
4. Введите JSON:

```
{
  "customer_id": 1,
  "items": [
    {
      "product_id": 1,
      "qty": 2
    }
  ]
}
```

5. Нажмите **Execute**

**Через командную строку (curl):**

```
curl -X POST "http://127.0.0.1:8000/orders/" \
  -H "Content-Type: application/json" \
  -d '{
    "customer_id": 1,
    "items": [
      {"product_id": 1, "qty": 2},
      {"product_id": 5, "qty": 1}
    ]
  }'
```

---

### Шаг 4: Подтверждение заказа

После создания заказа вы получите ответ:

```
{
  "order_id": 51,
  "customer_id": 1,
  "order_date": "2025-11-18",
  "total_amount": 934.30,
  "items": [
    {
      "product_id": 1,
      "qty": 2,
      "unit_price": 467.15,
      "subtotal": 934.30
    }
  ]
}
```

**Сохраните `order_id`** — это номер вашего заказа!

---

### Шаг 5: Проверка заказа

Вы можете проверить детали заказа по его ID:

```
http://127.0.0.1:8000/orders/51
```

Или через Swagger UI:
1. Откройте http://127.0.0.1:8000/docs
2. Найдите **GET /orders/{order_id}**
3. Введите номер заказа
4. Нажмите **Execute**

---

## ⚠️ Возможные ошибки

### Ошибка 404: "Товар не найден"
**Причина:** Указан несуществующий `product_id`  
**Решение:** Проверьте список товаров через GET /products

### Ошибка 400: "Недостаточно товара"
**Причина:** На складе нет запрошенного количества товара  
**Решение:** Уменьшите количество (`qty`) или выберите другой товар

### Ошибка 422: "Validation Error"
**Причина:** Неверный формат запроса  
**Решение:** Проверьте, что JSON соответствует примеру

---

## 💡 Советы

✅ Используйте **Swagger UI** (http://127.0.0.1:8000/docs) — там удобнее тестировать API  
✅ Перед заказом проверьте наличие товара (поле `stock`)  
✅ Можно заказать несколько товаров одновременно — просто добавьте их в массив `items`

---

## 📞 Поддержка

Если возникли проблемы, проверьте:
1. Запущен ли сервер (`uvicorn main:app --reload`)
2. Импортированы ли данные (`python run_import.py`)
3. Доступна ли документация http://127.0.0.1:8000/docs

---

**Готово! Теперь вы можете оформлять заказы через API!** 🚀
```

***

**Сохрани оба файла в корень проекта!** 📁

***
