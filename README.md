```markdown
# OrderManager — мини-система заказов

Система управления заказами для малого интернет-магазина с API для работы с продуктами и заказами.

---

## 📁 Структура проекта

```
OrderManager/
├── main.py                  # Главный файл FastAPI приложения
├── database.py              # Настройка подключения к БД
├── models.py                # SQLAlchemy модели (Products, Orders, OrderItems)
├── schemas.py               # Pydantic схемы для валидации
├── run_import.py            # Скрипт импорта данных из CSV
├── routers/
│   ├── products.py          # Эндпоинты для продуктов
│   └── orders.py            # Эндпоинты для заказов
├── convert_customers.py     # Импорт клиентов из CSV
├── convert_products.py      # Импорт продуктов из CSV
├── convert_orders.py        # Импорт заказов из CSV
├── data/
│   ├── customers.csv        # Исходные данные клиентов
│   ├── products.csv         # Исходные данные продуктов
│   └── sample_orders.csv    # Исходные данные заказов
├── OrderManager.db          # База данных SQLite
├── ERD.png                  # Схема базы данных
└── short_spec.md            # Техническая спецификация
```

---

## 🚀 Установка и запуск

### 1. Установка зависимостей

```
pip install fastapi uvicorn sqlalchemy pydantic
```

### 2. Импорт данных в БД

```
python run_import.py
```

Скрипт создаст базу данных `OrderManager.db` и импортирует:
- 300 продуктов из `products.csv`
- 200 клиентов из `customers.csv`
- 50 заказов из `sample_orders.csv`

### 3. Запуск сервера

```
uvicorn main:app --reload
```

Сервер запустится на: **http://127.0.0.1:8000**

### 4. Документация API

Откройте в браузере: **http://127.0.0.1:8000/docs**

---

## 📡 API Endpoints

### **GET /products**
Получить список продуктов с фильтрацией и пагинацией.

**Query параметры:**
- `page` — номер страницы (default: 1)
- `per_page` — количество на странице (default: 10)
- `category` — фильтр по категории
- `price_min` — минимальная цена
- `price_max` — максимальная цена

**Пример запроса:**
```
curl "http://127.0.0.1:8000/products?page=1&per_page=5&category=Electronics"
```

**Ответ:**
```
{
  "total": 300,
  "page": 1,
  "per_page": 5,
  "products": [
    {
      "product_id": 1,
      "name": "Speed Item 1",
      "category": "Electronics",
      "price": 467.15,
      "stock": 45
    }
  ]
}
```

---

### **POST /orders**
Создать новый заказ.

**Body:**
```
{
  "customer_id": 1,
  "items": [
    {
      "product_id": 1,
      "qty": 2
    }
  ]
}
```

**Пример запроса:**
```
curl -X POST "http://127.0.0.1:8000/orders/" \
  -H "Content-Type: application/json" \
  -d '{
    "customer_id": 1,
    "items": [{"product_id": 1, "qty": 2}]
  }'
```

**Ответ:**
```
{
  "order_id": 51,
  "customer_id": 1,
  "order_date": "2025-11-18",
  "total_amount": 934.30,
  "items": [
    {
      "product_id": 1,
      "qty": 2,
      "unit_price": 467.15,
      "subtotal": 934.30
    }
  ]
}
```

---

### **GET /orders/{order_id}**
Получить информацию о заказе.

**Пример запроса:**
```
curl "http://127.0.0.1:8000/orders/1"
```

**Ответ:**
```
{
  "order_id": 1,
  "customer_id": 42,
  "order_date": "2025-09-15",
  "total_amount": 1234.56,
  "items": [
    {
      "product_id": 5,
      "qty": 3,
      "unit_price": 411.52,
      "subtotal": 1234.56
    }
  ]
}
```

---

## 🛠 Технологии

- **FastAPI** — веб-фреймворк
- **SQLAlchemy** — ORM для работы с БД
- **SQLite** — база данных
- **Pydantic** — валидация данных
- **Uvicorn** — ASGI сервер

---

## 📝 База данных

### Таблицы:
- **Products** — каталог товаров
- **Customers** — список клиентов
- **Orders** — заказы
- **OrderItems** — товары в заказе

ERD схема: см. `ERD.png`

---

## 📚 Дополнительная документация

- `user_guide.md` — инструкция для пользователей
- `short_spec.md` — техническая спецификация
- Swagger UI: http://127.0.0.1:8000/docs

---

## 🎯 Автор

Проект разработан в рамках задания "OrderManager — мини-система заказов"
```

